# CWS screenshots — to capture before submission (task 7.6)

Four 1280×800 PNGs, captured from a clean Chrome profile with the packed
extension loaded:

1. `01_popup_event.png` — popup showing COVID crash auto-detection on a news
   article
2. `02_popup_results.png` — equity chart + metric cards after Run Backtest
3. `03_settings.png` — settings page with API base URL and key fields
4. `04_fab_injection.png` — candlestick FAB on a financial news page

Capture: load the zip from `scripts/pack_extension.sh` via
`chrome://extensions`, set the window to 1280×800 (or crop), and screenshot
with `⌘⇧4` → space → window.
