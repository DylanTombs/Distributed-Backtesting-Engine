# Development + CI dependencies: the API runtime plus test and lint tooling.
#
# The product path (extension → API → C++ backtester) imports no torch,
# sklearn, or streamlit. The legacy transformer training code under
# research/transformer/ is kept as a build record only and is not exercised
# by the test suite — install requirements-legacy.txt if you need to run it.
numpy>=1.21.0
pandas>=1.3.0
pydantic>=2.0
fastapi>=0.111
uvicorn[standard]>=0.30
slowapi>=0.1.9
httpx>=0.27
trafilatura>=1.10
dateparser>=1.2
anthropic>=0.28

# Test + lint tooling
pytest>=7.0.0
pytest-cov>=4.0.0
flake8>=3.9.0
